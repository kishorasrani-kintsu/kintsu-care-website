# Kintsu Care website is a template-less static multi-page site: every page
# repeats the same header/nav/ticker/footer markup by design (no includes or
# build step). That boilerplate is what's driving the duplication metric, not
# a real code smell, so duplication detection is excluded for HTML pages.
sonar.cpd.exclusions=**/*.html
